# 🌿 Herb App Git Attributes
# Keeps text encoding and line endings consistent across all systems.

# ──────────────────────────────
# Default behavior: treat all text files as UTF-8 with LF line endings
* text eol=lf
* encoding=utf-8

# ──────────────────────────────
# Force UTF-8 for key file types
*.json text working-tree-encoding=UTF-8
*.html text working-tree-encoding=UTF-8
*.css text working-tree-encoding=UTF-8
*.js text working-tree-encoding=UTF-8
*.md text working-tree-encoding=UTF-8
*.yml text working-tree-encoding=UTF-8

# ──────────────────────────────
# Handle Windows vs Unix line endings
*.bat text eol=crlf
*.sh text eol=lf

# ──────────────────────────────
# Binary files (do not diff or alter)
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.svg binary
*.ico binary
*.ttf binary
*.otf binary
*.woff binary
*.woff2 binary
*.db binary
*.sqlite binary
*.xlsx binary
*.xls binary
*.ods binary

# ──────────────────────────────
# End of file
